# 关闭缓存, 即时刷新
#  spring:
#    thymeleaf:
#      cache: false

# 热部署生效spring:
# 设置重启的目录,添加那个目录的文件需要restart
# 为mybatis设置，生产环境可删除
# 排除那个目录的文件不需要restart
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
#      include:
#        mapper: /mapper-[\\w-\\.]+jar
#        pagehelper: /pagehelper-[\\w-\\.]+jar
#     exclude: static/**, public/**, WEB-INF/**

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/kingj?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: 123456

# Mybatis分页插件配置
  pagehelper:
    dialect: mysql
    resonable: true
    supportMethodsArguments: true
    params: count=countSql



